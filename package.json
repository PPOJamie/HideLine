{
  "name": "hideline-london-companion",
  "version": "2.3.4",
  "private": true,
  "description": "A London hide-and-seek companion that centres every hiding circle on the supplied official game-map pins and uses deployment-generated OpenStreetMap shoreline geometry for Body of Water deductions.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "start": "node scripts/dev-server.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate:data": "node scripts/validate-data.mjs",
    "validate:layout": "node scripts/validate-layout.mjs",
    "validate": "npm run validate:data && npm run validate:layout",
    "check": "npm run validate && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "hide-and-seek",
    "london",
    "transit-game",
    "pwa",
    "supabase",
    "github-pages"
  ],
  "license": "MIT"
}
