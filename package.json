{
  "name": "hideline-london-companion",
  "version": "2.0.0",
  "private": true,
  "description": "A simple, installable London hide-and-seek companion with timers, questions, a live deduction map and optional team sync.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "start": "node scripts/dev-server.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate-data.mjs",
    "check": "npm run validate && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "hide-and-seek",
    "london",
    "transit-game",
    "pwa",
    "supabase",
    "github-pages"
  ],
  "license": "MIT"
}
