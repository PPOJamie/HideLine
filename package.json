{
  "name": "hideline-london-companion",
  "version": "2.3.2",
  "private": true,
  "description": "A London hide-and-seek companion with a map-only Body of Water calculator, reliable boundaries, precise timers and real-time multiplayer sync.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "start": "node scripts/dev-server.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate:data": "node scripts/validate-data.mjs",
    "validate:layout": "node scripts/validate-layout.mjs",
    "validate": "npm run validate:data && npm run validate:layout",
    "check": "npm run validate && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "hide-and-seek",
    "london",
    "transit-game",
    "pwa",
    "supabase",
    "github-pages"
  ],
  "license": "MIT"
}
